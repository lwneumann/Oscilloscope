```Scope``` is a new approach of using python to generate the waveforms as opposed to the C++ alternatives in the other folder. So far it doesn't actually do that though as with the C++ there was a significant amount I could have abstracted and it would have made making new shapes overall significantly easier. Because of this I'm focusing entirely on an interface for the user first then will make the waveforms and premade shapes and patterns second once the actual structure has been properly fleshed out.

```TestLogic``` is proof of concept using pygame to visualize some ideas for rendering things. It's pretty slow for some of them due to the way that I am drawing and fading each point to poorly immitate an oscilloscope. You can certainly use osirender and other libraries that would be siginificantly more adept at doing this but I was on a plane when I started working on it and so couldn't install it or check documentation so here we are.